---
import { testimonials } from "../data/site";
---

<section class="section section-soft" id="testimonials" aria-labelledby="testimonials-heading">
  <div class="container">
    <div class="intro">
      <p class="eyebrow">Happy Homeowners</p>
      <h2 id="testimonials-heading" class="section-title">
        What Clients <span class="accent">Say</span>
      </h2>
    </div>
    <div class="grid">
      {
        testimonials.map((t, i) => (
          <article class:list={["card", i % 2 === 0 ? "card-pink" : "card-white"]}>
            <span class="quote-mark" aria-hidden="true">”</span>
            <div class="stars" aria-label="5 out of 5 stars">★★★★★</div>
            <p>“{t.quote}”</p>
            <div class="person">
              <span class="avatar" aria-hidden="true">{t.name.slice(0, 1)}</span>
              <strong>{t.name}</strong>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .intro {
    margin-bottom: 2.25rem;
  }

  .grid {
    display: grid;
    gap: 1.1rem;
  }

  .card {
    position: relative;
    border-radius: 20px;
    padding: 1.65rem 1.45rem 1.45rem;
    box-shadow: var(--shadow);
    min-height: 100%;
  }

  .card-pink {
    background: linear-gradient(155deg, var(--pink-hot), var(--pink-deep));
    color: white;
  }

  .card-white {
    background: white;
    border: 1px solid var(--line);
  }

  .quote-mark {
    position: absolute;
    top: 0.45rem;
    right: 1.1rem;
    font-size: 3.2rem;
    line-height: 1;
    color: rgba(26, 21, 35, 0.18);
    font-family: var(--font-display);
  }

  .card-pink .quote-mark {
    color: rgba(255, 255, 255, 0.28);
  }

  .stars {
    color: #f5c518;
    letter-spacing: 0.08em;
    margin-bottom: 0.75rem;
    font-size: 0.95rem;
  }

  p {
    margin: 0 0 1.35rem;
    font-size: 0.98rem;
    line-height: 1.6;
  }

  .person {
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .avatar {
    width: 38px;
    height: 38px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    font-weight: 700;
    background: rgba(255, 255, 255, 0.22);
  }

  .card-white .avatar {
    background: var(--pink-soft);
    color: var(--pink-deep);
  }

  @media (min-width: 700px) {
    .grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 980px) {
    .grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
